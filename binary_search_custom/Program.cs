using System;

namespace binary_search_custom
{
    /* 
     * Custom implementation for binary search
     * p - index of first element in the arrary for algorithm
     * r - index of last element in the array for algorithm
     * q - index of the middle of array to search
     * a - variable that points to array being search
     * n = a.length - 1 
     */
    class MainClass
    {
        public static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");
        }
    }
}
